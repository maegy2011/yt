generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(cuid())
  email     String   @unique
  name      String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Post {
  id        String   @id @default(cuid())
  title     String
  content   String?
  published Boolean  @default(false)
  authorId  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model WatchedVideo {
  id          String   @id @default(cuid())
  videoId     String   @unique
  title       String
  channelName String
  thumbnail   String
  duration    String?
  viewCount   String?
  watchedAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model FavoriteVideo {
  id          String   @id @default(cuid())
  videoId     String   @unique
  title       String
  channelName String
  thumbnail   String
  duration    String?
  viewCount   String?
  addedAt     DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model FavoriteChannel {
  id              String   @id @default(cuid())
  channelId       String   @unique
  name            String
  thumbnail       String
  subscriberCount String?
  videoCount      String?
  viewCount       String?
  addedAt         DateTime @default(now())
  updatedAt       DateTime @updatedAt
}

model Notebook {
  id          String      @id @default(cuid())
  title       String
  description String?
  color       String      @default("#3b82f6")
  isPublic    Boolean     @default(false)
  tags        String      @default("")
  category    String      @default("general")
  thumbnail   String?
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
  notes       VideoNote[]
  noteLinks   NoteLink[]
  pdfs        PdfFile[]
}

model VideoNote {
  id          String    @id @default(cuid())
  videoId     String
  title       String
  channelName String
  thumbnail   String
  note        String
  fontSize    Int       @default(16)
  startTime   Int?
  endTime     Int?
  isClip      Boolean   @default(false)
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  notebookId  String?
  notebook    Notebook? @relation(fields: [notebookId], references: [id])
  noteLinks   NoteLink[]
}

model NoteLink {
  id         String   @id @default(cuid())
  noteId     String
  notebookId String
  createdAt  DateTime @default(now())
  
  note     VideoNote @relation(fields: [noteId], references: [id], onDelete: Cascade)
  notebook Notebook  @relation(fields: [notebookId], references: [id], onDelete: Cascade)
  
  @@unique([noteId, notebookId])
}

model VideoPlaybackPosition {
  id          String   @id @default(cuid())
  videoId     String   @unique
  title       String
  channelName String
  thumbnail   String
  duration    Float
  currentTime Float    @default(0)
  lastWatched DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model PdfFile {
  id          String   @id @default(cuid())
  notebookId  String
  filename    String
  originalName String
  size        Int
  mimeType    String
  path        String
  uploadedAt  DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  notebook    Notebook @relation(fields: [notebookId], references: [id], onDelete: Cascade)
  
  @@index([notebookId])
}

model BlacklistedItem {
  id          String   @id @default(cuid())
  itemId      String   @unique
  title       String
  type        String   // 'video' | 'playlist' | 'channel'
  thumbnail   String?
  channelName String?
  addedAt     DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  // Performance optimizations for large-scale blacklisting
  videoHash   String?  // MD5 hash of video ID for faster lookups
  channelHash String?  // MD5 hash of channel ID for channel blocking
  isChannelBlock Boolean @default(false) // Quick filter for channel-level blocks
  priority    Int      @default(0) // Priority for conflict resolution
  batchId     String?  // Track batch imports for management
  
  @@index([itemId])
  @@index([type])
  @@index([videoHash])
  @@index([channelHash])
  @@index([isChannelBlock, type])
  @@index([addedAt])
  @@index([priority])
}

model WhitelistedItem {
  id          String   @id @default(cuid())
  itemId      String   @unique
  title       String
  type        String   // 'video' | 'playlist' | 'channel'
  thumbnail   String?
  channelName String?
  addedAt     DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  // Performance optimizations for large-scale whitelisting
  videoHash   String?  // MD5 hash of video ID for faster lookups
  channelHash String?  // MD5 hash of channel ID for channel whitelisting
  isChannelWhitelist Boolean @default(false) // Quick filter for channel-level whitelists
  priority    Int      @default(0) // Priority for conflict resolution
  batchId     String?  // Track batch imports for management
  
  @@index([itemId])
  @@index([type])
  @@index([videoHash])
  @@index([channelHash])
  @@index([isChannelWhitelist, type])
  @@index([addedAt])
  @@index([priority])
}

// New model for pattern-based blocking
model BlacklistPattern {
  id          String   @id @default(cuid())
  pattern     String   // Regex pattern or keyword
  type        String   // 'title' | 'channel' | 'description' | 'tags'
  patternType String   @default("keyword") // 'keyword' | 'regex' | 'wildcard'
  isActive    Boolean  @default(true)
  priority    Int      @default(0)
  matchCount  Int      @default(0) // Track usage statistics
  lastMatched DateTime?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  @@index([type, isActive])
  @@index([priority])
  @@index([patternType])
}

// New model for bulk import tracking
model BlacklistBatch {
  id          String   @id @default(cuid())
  name        String?  // Optional batch name
  description String?
  source      String   // 'file' | 'api' | 'manual'
  itemCount   Int      @default(0)
  successCount Int      @default(0)
  errorCount  Int      @default(0)
  status      String   @default("pending") // 'pending' | 'processing' | 'completed' | 'failed'
  filePath    String?  // For file imports
  metadata    String?  // JSON metadata
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  @@index([status])
  @@index([createdAt])
  @@index([source])
}

// New model for blacklist performance metrics
model BlacklistMetrics {
  id          String   @id @default(cuid())
  date        DateTime @default(now())
  totalItems  Int      @default(0)
  videoCount  Int      @default(0)
  channelCount Int     @default(0)
  playlistCount Int    @default(0)
  filterHits  Int      @default(0) // How many times blacklist was applied
  avgFilterTime Float   @default(0) // Average filtering time in ms
  cacheHitRate Float    @default(0) // Cache hit rate percentage
  
  @@unique([date])
  @@index([date])
}
